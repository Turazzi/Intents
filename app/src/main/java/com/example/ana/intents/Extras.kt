package com.example.ana.intents

/**
 * Objeto que armazena constantes para serem utilizadas como chaves em Intents.
 * No Android, quando passamos dados entre Activities ou Fragments usando Intent,
 * utilizamos chaves para recuperar os valores corretamente.
 */
object Extras {
    /**
     * Constante utilizada como chave para passar parâmetros via Intent.
     */
    const val PARAMETER_EXTRA = "PARAMETER"
}
